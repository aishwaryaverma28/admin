import React from 'react'
import { useParams, Link } from "react-router-dom";

const DealUpdate = () => {
    const { id } = useParams();
  return (
    <div>DealUpdate</div>
  )
}

export default DealUpdate